{
  "private": true,
  "scripts": {
    "build": "npx prisma generate --schema=./apps/backend/prisma/schema.prisma && turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "eslint-config-custom": "*",
    "prettier": "^3.0.3",
    "turbo": "latest"
  },
  "name": "woolshoes",
  "packageManager": "yarn@1.22.19",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "0.0.0"
}
